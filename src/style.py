"""!
@file style.py
@brief Color palette, fonts and shared constants for the BoaPDF UI.
"""

colorBg = "#f5f7f6"          # window background
colorCard = "#ffffff"        # cards, panels, inputs
colorInk = "#1d2839"         # primary text
colorMuted = "#6b7f8a"       # secondary text
colorBrand = "#22a06b"       # accent / primary buttons
colorBrandDark = "#1b8a59"  # primary button hover
colorTint = "#e9f7f0"        # faint brand tint (hover surfaces)
colorLine = "#e1e8e4"        # hairline borders
colorGreen = "#22a06b"       # success (same as brand)
colorDisabled = "#c4d1ca"    # disabled controls
colorGhost = "#ecf1ee"       # secondary (ghost) buttons
colorGhostHover = "#dfe6e1"  # ghost button hover

fontFamily = "Segoe UI"
radiusButton = 11        # corner radius for buttons / rounded shapes
radiusCard = 8    # corner radius for cards
thumbWidth = 130  # thumbnail width in px
gridCols = 5      # thumbnails per row
